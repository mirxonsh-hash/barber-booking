Flask==2.3.3
Flask-CORS==4.0.0
psycopg2-binary==2.9.10
python-dotenv==1.0.0
SQLAlchemy==2.0.23
PyJWT==2.8.0
requests==2.31.0

# Для продакшена на Render/Vercel/Heroku
gunicorn==21.2.0

# Для удобной работы с датами и временем
python-dateutil==2.8.2
pytz==2024.1

# Для улучшенного логирования (опционально)
structlog==24.1.0

# Для хеширования паролей (опционально, если будете делать)
bcrypt==4.1.2

# Для валидации данных (опционально)
marshmallow==3.20.1
marshmallow-sqlalchemy==0.29.0

# Для тестирования (опционально)
pytest==7.4.3
pytest-flask==1.2.0

# Утилиты для разработки
click==8.1.7
Werkzeug==2.3.8
itsdangerous==2.1.2
Jinja2==3.1.2
MarkupSafe==2.1.5

# Для работы с async (если понадобится в будущем)
aiohttp==3.9.1

# Для отправки email уведомлений (опционально)
Flask-Mail==0.9.1

# Кэширование (опционально)
Flask-Caching==2.1.0

# Для генерации QR кодов (опционально, для кодов барберов)
qrcode[pil]==7.4.2
Pillow==10.2.0
